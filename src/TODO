--- TODO LIST ---
:: Tasks
set_function, set_method should accept a table
add tests for find_duplicate, primes computation, etc...

:: Modules
- 'range' module to be used with matrix modules

- matrix module add-on (once range is available)
  rand (after LuaJIT random), randn, linspace, logspace
  mean, std, cov, corr, conv

:: C Embbeding
- Readline or LuaPrompt to be included for better interactive input?
