--- TODO LIST ---

:: Modules
- 'range' module to be used with matrix modules

- matrix module add-on (once range is available)
  rand (after LuaJIT random), randn, linspace,
  cosd, sind, acosd, asind
  mean, std, cov, corr, conv

:: C Embbeding
- interactive mode with proper backtracing (after luajit.c)
- Readline or LuaPrompt to be included for better interactive input?

:: Packages
- LuaUnit or Busted to be included for (external) unit testing
- LPEG to be included for regular expression parsing/selection
- NLOPT (or other libs) to be included for non-linear optimization with constraints