Package: luajit-2.1-alpha

URL:
    http://luajit.org

Install:
    edit src/Makefile, uncomment XCFLAGS+= -DLUAJIT_ENABLE_LUA52COMPAT
    optionaly set HASH_BIAS to 0 in lj_tab.h
    export MACOSX_DEPLOYMENT_TARGET=10.6

    git submodule update --remote --merge
    make clean
    make amalg
    make install PREFIX="`pwd`/OSX"
    make install PREFIX="`pwd`/Linux"
    make install PREFIX="`pwd`/Windows"

Git info:
    http://git-scm.com/book/en/v2/Git-Tools-Submodules