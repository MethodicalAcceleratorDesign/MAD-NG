MADX:open_env()

-- change RBEND to SBEND

mbrb :set_parent(sbend)
mbrc :set_parent(sbend)
mbrs :set_parent(sbend)
mbw  :set_parent(sbend)
mbx  :set_parent(sbend)
mbxw :set_parent(sbend)

MADX:close_env()
